<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Meu Perfil - MicroLearn</title>
    <link rel="stylesheet" href="/stylesheets/dashboard.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <style>
      /* Basic styles from dashboard.css or similar */
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #f8f9fa;
      }

      .navbar {
        background: #f3eaff;
        padding: 1rem 2rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }
      .nav-left,
      .nav-center,
      .nav-right {
        display: flex;
        align-items: center;
      }
      .nav-logo {
        height: 40px;
        margin-right: 1rem;
      }
      .nav-link {
        color: #222;
        text-decoration: none;
        margin: 0 1rem;
        font-weight: 500;
        font-size: 1rem;
      }
      .nav-link:hover {
        color: #7950f2;
      }
      .profile-icon {
        font-size: 2rem;
        color: #7950f2;
        margin-left: 1rem;
      }
      .search-bar {
        display: flex;
        align-items: center;
        background: #fff;
        border-radius: 6px;
        padding: 0.2rem 0.5rem;
        box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
      }
      .search-bar input {
        border: none;
        outline: none;
        padding: 0.4rem;
        font-size: 1rem;
        background: transparent;
      }
      .search-button {
        background: none;
        border: none;
        color: #7950f2;
        font-size: 1.2rem;
        cursor: pointer;
      }

      .page-title {
        background: #f3eaff;
        padding: 1.5rem 0 1rem 0;
        text-align: center;
        font-size: 1.2rem;
        color: #222;
        margin-bottom: 2rem;
      }

      .profile-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 1rem;
        display: flex;
        gap: 2rem;
      }

      .sidebar {
        flex: 0 0 200px; /* Fixed width for sidebar */
        background: #f3eaff;
        border-radius: 8px;
        padding: 1rem;
        box-shadow: 0 2px 8px rgba(121, 80, 242, 0.05);
      }

      .sidebar-link {
        display: block;
        text-decoration: none;
        color: #222;
        padding: 0.8rem 1rem;
        margin-bottom: 0.5rem;
        border-radius: 4px;
        transition: background-color 0.2s, color 0.2s;
      }

      .sidebar-link:hover {
        background-color: #e9d8ff;
        color: #7950f2;
      }

      .sidebar-link.active {
        background-color: #7950f2;
        color: white;
        font-weight: bold;
      }

      .sidebar-link.logout {
        background-color: #ff6b6b;
        color: white;
        font-weight: bold;
        text-align: center;
        margin-top: 2rem; /* Space above logout button */
      }
      .sidebar-link.logout:hover {
        background-color: #ff4c4c;
      }

      .profile-content {
        flex-grow: 1;
        background: #fff;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      }

      .profile-content h3 {
        color: #7950f2;
        margin-bottom: 1.5rem;
        font-size: 1.4rem;
      }

      .profile-form .form-group {
        margin-bottom: 1.5rem;
      }

      .profile-form label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: 500;
        color: #555;
      }

      .profile-form input[type="text"],
      .profile-form input[type="email"],
      .profile-form input[type="tel"] {
        width: 100%;
        padding: 0.8rem;
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 1rem;
        box-sizing: border-box; /* Include padding and border in element's total width and height */
      }

      .profile-form .profile-picture-upload {
        display: flex;
        align-items: center;
        margin-bottom: 2rem;
      }

      .profile-form .profile-picture-placeholder {
        width: 80px;
        height: 80px;
        background-color: #ddd;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        color: #666;
        margin-right: 1.5rem;
      }

      .profile-form button {
        padding: 0.8rem 1.5rem;
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.2s;
      }

      .profile-form .btn-primary {
        background-color: #7950f2;
        color: white;
      }
      .profile-form .btn-primary:hover {
        background-color: #5f3dc4;
      }

      .profile-form .btn-secondary {
        background-color: #ccc;
        color: #333;
        margin-right: 1rem;
      }
      .profile-form .btn-secondary:hover {
        background-color: #bbb;
      }

      .profile-form .form-actions {
        text-align: right;
      }
    </style>
  </head>
  <body
    id="top"
    class="<%= user && user.theme === 'dark' ? 'dark-mode' : '' %>"
    data-user-logged="<%= user ? 'true' : 'false' %>"
  >
    <!-- Navbar -->
    <nav class="navbar">
      <div class="nav-left">
        <img
          src="/images/logo-microlearn.png"
          alt="MicroLearn Logo"
          class="nav-logo"
        />
        <a href="/" class="nav-link">HOME</a>
        <a href="/perguntas" class="nav-link">Perguntas</a>
      </div>
      <div class="nav-center">
        <div class="search-bar">
          <input type="text" placeholder="Pesquisar..." />
          <button type="button" class="search-button">
            <i class="fas fa-search"></i>
          </button>
        </div>
      </div>
      <div class="nav-right">
        <a href="/minhas-tarefas" class="nav-link">Minhas Tarefas</a>
        <% if (user && user.foto_perfil) { %>
          <a href="/profile" class="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center overflow-hidden">
            <img src="<%= user.foto_perfil %>" alt="Foto de Perfil" style="width:40px;height:40px;object-fit:cover;border-radius:50%;" />
          </a>
        <% } else { %>
          <a href="/profile" class="profile-icon">
            <i class="fa-solid fa-circle-user"></i>
          </a>
        <% } %>
      </div>
    </nav>

    <!-- Título da página -->
    <div class="page-title">Meu Perfil</div>

    <div class="profile-container">
      <!-- Sidebar de Navegação -->
      <div class="sidebar">
        <a href="/profile" class="sidebar-link active">Dados Pessoais</a>
        <a href="/profile/security" class="sidebar-link">Segurança</a>
        <a href="/profile/theme" class="sidebar-link">Tema</a>
        <a href="#" class="sidebar-link" id="progresso-link">Meu Progresso</a>
        <a href="/api/users/logout" class="sidebar-link logout" id="logout-link">Sair da Conta</a>
      </div>

      <!-- Conteúdo Principal do Perfil -->
      <div class="profile-content" id="dados-pessoais-content">
        <h3>Dados Pessoais</h3>

        <form class="profile-form" id="profile-form" enctype="multipart/form-data">
          <div class="profile-picture-upload">
            <div class="profile-picture-placeholder" id="profile-picture-preview">
              <% if (user && user.foto_perfil) { %>
                <img src="<%= user.foto_perfil %>" alt="Foto de Perfil" style="width:80px;height:80px;border-radius:50%;object-fit:cover;" />
              <% } else { %>
                <i class="fa-solid fa-user"></i>
              <% } %>
            </div>
            <input type="file" id="foto-perfil-input" name="foto" accept="image/*" style="display:none;" />
            <button type="button" class="btn-primary" id="alterar-foto-btn">Alterar Foto</button>
          </div>

          <div class="form-group">
            <label for="nome">Nome completo</label>
            <input
              type="text"
              id="nome"
              name="nome"
              placeholder="Seu nome"
              value="<%= user && user.nome ? user.nome : '' %>"
            />
          </div>

          <div class="form-group">
            <label for="email">E-mail</label>
            <input
              type="email"
              id="email"
              name="email"
              placeholder="seu.email@example.com"
              value="<%= user && user.email ? user.email : '' %>"
            />
          </div>

          <div class="form-group">
            <label for="telefone">Telefone</label>
            <input
              type="tel"
              id="telefone"
              name="telefone"
              placeholder="(XX) XXXXX-XXXX"
              value="<%= user && user.telefone ? user.telefone : '' %>"
            />
          </div>

          <div class="form-actions">
            <button type="button" class="btn-secondary">Cancelar</button>
            <button type="submit" class="btn-primary" id="salvar-perfil-btn">Salvar</button>
          </div>
        </form>
      </div>
      <div class="profile-content" id="progresso-content" style="display:none;">
        <div class="page-title">Meu progresso</div>
        <div class="progresso-box" style="background: var(--progresso-bg, #f8f0fc); border-radius: 16px; padding: 2rem; max-width: 400px; margin: 0 auto; box-shadow: 0 2px 8px rgba(121,80,242,0.08);">
          <div style="display: flex; justify-content: space-between; margin-bottom: 1.5rem;">
            <div>
              <div style="color: #7950f2; font-size: 1.2rem; font-weight: bold;">Vídeos Assistidos</div>
              <div id="videos-assistidos" style="font-size: 2.2rem; color: #7950f2;">0</div>
            </div>
            <div>
              <div style="color: #7950f2; font-size: 1.2rem; font-weight: bold;">Tarefas concluídas</div>
              <div id="tarefas-concluidas" style="font-size: 2.2rem; color: #7950f2;">0</div>
            </div>
          </div>
          <div id="progresso-materias"></div>
          <button id="voltar-progresso" style="margin-top: 2rem; background: #7950f2; color: white; border: none; border-radius: 6px; padding: 0.5rem 2rem; font-size: 1rem; cursor: pointer;">Voltar</button>
        </div>
      </div>
      <script>
        // Alternar abas do perfil
        function mostrarAba(idAba, linkSidebar) {
          // Esconde todas as abas
          document.querySelectorAll('.profile-content').forEach(div => div.style.display = 'none');
          // Remove active de todos os links
          document.querySelectorAll('.sidebar-link').forEach(link => link.classList.remove('active'));
          // Mostra a aba desejada
          document.getElementById(idAba).style.display = '';
          // Marca o link como ativo
          linkSidebar.classList.add('active');
          // Tema escuro para progresso
          if(idAba === 'progresso-content') {
            if(document.body.classList.contains('dark-mode')) {
              document.querySelector('.progresso-box').style.setProperty('background', '#222');
              document.querySelector('.progresso-box').style.setProperty('color', '#f0f0f0');
            } else {
              document.querySelector('.progresso-box').style.setProperty('background', '#f8f0fc');
              document.querySelector('.progresso-box').style.setProperty('color', '#222');
            }
          }
        }
        // Sidebar links
        if (window.location.pathname === '/profile') {
          document.querySelectorAll('.sidebar-link').forEach(link => {
            link.addEventListener('click', function(e) {
              if(this.id === 'progresso-link') {
                e.preventDefault();
                mostrarAba('progresso-content', this);
                carregarProgresso();
              } else if(this.textContent.includes('Dados Pessoais')) {
                e.preventDefault();
                mostrarAba('dados-pessoais-content', this);
              }
              // Segurança e Tema deixam o comportamento padrão (recarregam a página)
            });
          });
          // Botão voltar do progresso
          document.getElementById('voltar-progresso').addEventListener('click', function() {
            const dadosPessoaisLink = Array.from(document.querySelectorAll('.sidebar-link')).find(l => l.textContent.includes('Dados Pessoais'));
            mostrarAba('dados-pessoais-content', dadosPessoaisLink);
          });
        }
        // Buscar progresso do usuário
        async function carregarProgresso() {
          const response = await fetch('/api/users/progresso');
          const data = await response.json();
          document.getElementById('videos-assistidos').innerText = data.videosAssistidos;
          document.getElementById('tarefas-concluidas').innerText = data.tarefasConcluidas;
          let html = '';
          data.progressoMaterias.forEach(m => {
            html += `<div style='display:flex;justify-content:space-between;align-items:center;margin-bottom:0.7rem;'>
              <span style='color:#7950f2;font-weight:bold;'>${m.materia}</span>
              <span style='color:#7950f2;font-size:1.2rem;'>${m.porcentagem}%</span>
            </div>`;
          });
          document.getElementById('progresso-materias').innerHTML = html;
        }
        // Abrir aba de progresso automaticamente se vier de ?aba=progresso
        if (window.location.search.includes('aba=progresso')) {
          const progressoLink = document.getElementById('progresso-link');
          mostrarAba('progresso-content', progressoLink);
          carregarProgresso();
        }
        // Preview da foto ao selecionar
        document.getElementById('alterar-foto-btn').addEventListener('click', function() {
          document.getElementById('foto-perfil-input').click();
        });
        document.getElementById('foto-perfil-input').addEventListener('change', function() {
          const file = this.files[0];
          if (!file) return;
          const reader = new FileReader();
          reader.onload = function(e) {
            const preview = document.getElementById('profile-picture-preview');
            preview.innerHTML = `<img src='${e.target.result}' alt='Foto de Perfil' style='width:80px;height:80px;border-radius:50%;object-fit:cover;' />`;
          };
          reader.readAsDataURL(file);
        });
        // Envio do formulário completo (dados + foto)
        document.getElementById('profile-form').addEventListener('submit', async function(e) {
          e.preventDefault();
          const form = e.target;
          const formData = new FormData(form);
          const btn = document.getElementById('salvar-perfil-btn');
          btn.disabled = true;
          btn.innerText = 'Salvando...';
          try {
            const response = await fetch('/api/users/atualizar-perfil', {
              method: 'POST',
              body: formData
            });
            const data = await response.json();
            if (data.sucesso) {
              window.location.reload();
            } else {
              alert(data.mensagem || 'Erro ao salvar perfil.');
            }
          } catch (e) {
            alert('Erro ao conectar com o servidor.');
          }
          btn.disabled = false;
          btn.innerText = 'Salvar';
        });
      </script>
    </div>

    <!-- Optional: Add a footer if needed -->
    <!-- Include your footer EJS partial here if you have one -->
  </body>
</html>
