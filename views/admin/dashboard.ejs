<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin - Dashboard</title>
  <style>
    body { background: #f3eaff; font-family: Arial, sans-serif; overflow: hidden; }
    .admin-container {
      max-width: 100vw;
      height: 100vh;
      margin: 0;
      border-radius: 0;
      box-shadow: none;
      display: flex;
      background: transparent;
    }
    .sidebar {
      flex: 0 0 180px; background: #f3eaff; border-radius: 12px 0 0 12px; padding: 2rem 1rem 1rem 1rem;
      display: flex; flex-direction: column; gap: 1.2rem; align-items: stretch;
    }
    .sidebar-title { color: #7950f2; font-size: 2rem; font-weight: bold; margin-bottom: 2rem; text-align: center; letter-spacing: 2px; }
    .sidebar-btn { background: #fff; color: #7950f2; border: none; border-radius: 6px; padding: 0.7rem 0.5rem; font-size: 1rem; margin-bottom: 0.5rem; cursor: pointer; text-align: left; transition: background 0.2s; }
    .sidebar-btn.active, .sidebar-btn:hover { background: #7950f2; color: #fff; font-weight: bold; }
    .sidebar-btn.logout { background: #ffb3b3; color: #fff; font-weight: bold; margin-top: 2rem; }
    .sidebar-btn.logout:hover { background: #ff4c4c; }
    .main-content {
      flex: 1;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }
    .dashboard-content-card {
      background: #fff;
      border-radius: 0 16px 16px 0;
      box-shadow: 0 2px 16px rgba(121, 80, 242, 0.10);
      width: 90%;
      max-width: 1100px;
      margin: 0 auto;
      padding: 2.5rem 2.5rem 2rem 2.5rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 80vh;
      overflow: hidden;
    }
    .dashboard-title {
      color: #7950f2;
      font-size: 1.7rem;
      font-weight: bold;
      margin-bottom: 1.5rem;
    }
    .stats-cards-row {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 2rem;
      width: 100%;
      justify-content: center;
    }
    .stat-card {
      background: #f3eaff;
      border-radius: 8px;
      padding: 1.2rem 2rem;
      text-align: center;
      min-width: 160px;
      flex: 1;
      margin: 0 0.2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      max-width: 220px;
    }
    .info-card {
      background: #e9d8ff;
    }
    .stat-value {
      color: #7950f2;
      font-size: 1.5rem;
      font-weight: bold;
    }
    .stat-label {
      color: #555;
      font-size: 1rem;
    }
    .recent-activity-title {
      color: #7950f2;
      font-size: 1.2rem;
      font-weight: bold;
      margin-bottom: 0.7rem;
      margin-top: 1.5rem;
      width: 100%;
      text-align: left;
    }
    .table-wrapper {
      width: 100%;
      overflow-x: auto;
      margin-bottom: 1.5rem;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
    }
    th, td {
      padding: 0.7rem 0.5rem;
      text-align: center;
    }
    th {
      background: #f3eaff;
      color: #7950f2;
      font-weight: bold;
    }
    tr:nth-child(even) {
      background: #f8f9fa;
    }
    .btn-voltar {
      background: #7950f2;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 0.7rem 2.5rem;
      font-size: 1rem;
      margin-top: 2rem;
      cursor: pointer;
      display: block;
      margin-left: auto;
      margin-right: auto;
    }
  </style>
</head>
<body>
  <div class="admin-container">
    <div class="sidebar">
      <div class="sidebar-title">
        <img src="/images/logo-microlearn.png" alt="Logo" style="height:40px;vertical-align:middle;margin-right:8px;">
        ADMIN
      </div>
      <button class="sidebar-btn">Conteúdo</button>
      <button class="sidebar-btn">Usuários</button>
      <button class="sidebar-btn">Configurações</button>
      <form action="/logout" method="GET" style="margin:0;">
        <button class="sidebar-btn logout" type="submit">Sair da Conta</button>
      </form>
    </div>
    <div class="main-content">
      <div class="dashboard-content-card">
        <div class="dashboard-title">Dashboard</div>
        <div class="stats-cards-row">
          <div class="stat-card">
            <div class="stat-value"><%= stats.usuarios %></div>
            <div class="stat-label">Usuários</div>
          </div>
          <div class="stat-card">
            <div class="stat-value"><%= stats.videos %></div>
            <div class="stat-label">Vídeos Publicados</div>
          </div>
          <div class="stat-card">
            <div class="stat-value"><%= stats.visualizacoes %></div>
            <div class="stat-label">Visualizações</div>
          </div>
        </div>
        <button class="btn-voltar" onclick="window.location.href='/'">Voltar</button>
      </div>
    </div>
  </div>
</body>
</html> 